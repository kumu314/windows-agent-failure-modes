# 怎么加一条（以及怎么不改一条）

这个包的读者**包括 agent**。规则设计的目标只有一个：让新增的条目不会变成第二份互相矛盾的手册。

## 什么时候值得写

满足下面任一条再动手：

- 一次排障花掉 **≥10 分钟**才定位到根因；
- 同一类失效在**同一环境里第二次**撞上；
- 错误信号与真实结论**方向相反**（假成功、假空结果、退出码 0 但事情没做成）。

反例：文档没写清、单纯记错了参数、只在你机器上出现过一次且无法复现——这些进对应工具的 issue，不进本包。

## 写之前：三条查重通道（按顺序走）

1. **列目录**：`ls skills/`。本包按**失效层次**分技能（网络 / 引用 / 编码 / 运行时 / 沙箱 / 陈旧输出），不按报错文案分，所以名字看起来总比你预想的宽一档。
2. **搜现象**：`grep -rn "<报错原文 | 退出码 | 字段名>" skills/`。注意大小写与单复数——`Battery` 命中不了 `DisallowStartIfOnBatteries`，这是本包自己踩过的假阴性。
3. **搜根因**：同一次故障常有好几种文案（"改了没生效" / "输出为空" / "退出码 0"），换措辞再搜一遍。

**判重标准**：如果你的条目和已有条目指向**同一个失效层**，那就是重复——往那个技能**追加一节**，不要新建一个同义词技能。

## 条目格式

固定四段：**现象 → 根因 → 对策 → 判定**。硬性要求：

- **判定可执行**：贴进终端就能跑；或者明确写出"读哪个字段、等于什么值即确诊"。
- 只写**实测**到的退出码、字节、字段名。没跑过的假设要么不写，要么标"未验证"。
- 现象里带**触发语境**（哪个 shell、哪个工具版本、中文还是英文区域），因为这些结论全部环境相关。
- 节末一行环境戳，格式固定：`- **验证于**：<OS 版本> · <shell 与版本> · <相关工具版本> · <日期>`。存量条目不批量补（补就是造数据），但你**新写的节必须有**。
- 确实**在这台机器上重跑不了**的节（要真代理缓存、要不可逆的远端删除、要带登录态的第三方后台……），用一行显式声明顶替戳：`- **状态**：复盘条目（<为什么复测不了>）`。括号里的理由是判据——只写 `复盘条目` 或 `（略）` 仍然报缺戳。这条是给"承认没测"用的，不是给"懒得测"用的。
- 节号只在技能内唯一；跨文件引用只写 `技能名 §N`，不写路径。裸 `§N`（不带技能名）会被 lint 挑出来——它一定会有读者按本文件的节号去读，然后读到完全另一条。
- 与已有结论同一失效层时可以追加一节；**改动别人的节**（修错、加适用范围）同样要带戳，并在新结论里说明你测到的与原文不一致在哪。

## `description` 的两条硬约束

`description` 不是一句简介，它是 agent 决定**要不要加载这颗技能**的唯一输入，因此两头都有限制：

- **长度**：客户端只把它的**前一段**喂给模型。本机实测最宽的可见窗口约 **276 字符**（Codex，技能根单开时）；`check.py` 按这个数报 warning——超出的尾部触发词等于没写，但**确切窗口是各家客户端自己的**，所以是 warning 不是 error，别拿一个机器的读数去挡别人的 PR。
- **YAML 写法**：写错了不是"显示不全"，是**整颗技能不加载且不报任何错**。三种形状 lint 现在按 error 拦：
  ① 正文含 ASCII 双引号却写成双引号标量（解析在内部第二个 `"` 处断）→ 正确写法是单引号标量 `description: '…含 "双引号" 的正文…'`；
  ② 单引号标量内部的 `'` 没写成 `''`；
  ③ 不加引号的值里出现 `: `（直接解析失败）或 ` #`（后半段被当注释丢掉，长度还看着正常）。
- **为什么单列一节**：修复前的 `main` 上，10 颗技能里 **3 颗**是 ① 那种写法，而 `check.py` **0 error**、CI **0 error**——因为 lint 用正则取 `key: value`，从不解析 YAML。**lint 绿不等于加载器绿**，这条判据补进来就是为了填这个缝。自查里 `selftest` 会对上述三类各验一次"必须响"、对合法写法验一次"不许响"。
- **验证于**：Windows 11 家庭中文版 10.0.26200 · Python 3.12.10 + PyYAML 6.x（复核解析结论用，`check.py` 本身不依赖它）· Codex 单技能根可见窗口 · 2026-09-20


## 提交前自查（这就是门槛）

```bash
python scripts/check.py selftest   # 先证明过滤器会响：脏样本必须全命中、净样本必须零误报
python scripts/check.py check      # 结构 / 节号 / 跨引用 / 行尾 / 出站红线
```

`check` 报 **0 error** 才提。warning 分两种：**你新增的那几条**要处理或在新 PR 里说明为什么不处理；
存量的（缺戳、描述过长等）不归你管，也不要顺手全量改——那会把两个主题混进一个 PR，正是本包不要的做法。

把自己机器的用户名、工作区目录名写进 `scripts/forbidden.local.txt`（一行一个，已 gitignore），lint 就会把它们当硬红线拦下来。**收录标准不是"看起来像路径"，而是"只在你这个人身上成立的字面量"**：真实姓名与内部称呼、非标准安装根目录名，和用户名同等收录——它们混在正文里时一点不像泄漏，本包就漏过一次（把维护者的名字写进"由××裁定"这类句子里，按路径词表扫不出来）。**不要改 `check.py` 去硬编码这些词**——那份文件是公开的，那样 lint 自己就成了泄漏点，正是本包出站红线要防的事。`selftest` 会按你本地清单的条数多验几类命中，命中不了就是清单没生效。

同样两条命令 CI 里也跑（`.github/workflows/lint.yml`：`ubuntu-latest` + Python 3.12，先 `selftest` 再 `check`）。lint 是纯文本扫描，不需要 Windows runner——虽然包里每一条结论都是 Windows 的。CI **故意不加 `--strict`**：存量里有几十节没有环境戳（实时数跑 `python scripts/check.py stats`，它按「节 − 有戳 − 豁免」给），按本协议不批量补（补 = 造数据），加了就是拿伪造读数换绿灯。**但要清楚 CI 拦不住哪一半**：`forbidden.local.txt` 是 gitignored 的，CI 拿不到它，所以远端只有通用四类（家目录、凭据形状、非默认代理端口、真实用户名）会响。**CI 绿 ≠ 你这个人没泄漏**——个人清单那一半只有本地跑才算数，出门前自己再扫一遍。

## 四条红线（碰了直接打回）

1. **真实身份痕迹**：用户名、家目录绝对路径、账号、代理软件的实际端口，**以及真实姓名 / 只有小范围人知道的称呼**（写流程时用人物角色代替，如"仓库维护者""发布方"）→ 一律换成 `<owner>`、`<端口>`、`<项目>` 占位符。
2. **凭据类内容**：token、cookie、会话文件、供调试用的浏览器 profile 的片段——这些文件里存的是**真**登录态，一个字符都不要出现。
3. **第三方数据**：赛题题目与参数、客户材料、内部项目细节。
4. **诱导破坏性动作的示例**：`--force`、`reset --hard`、`rm -rf` 出现时必须同时给出前置检查与回滚路径，否则不合并。

## 有冲突时：不要覆盖

与本包已有结论相左时，**保留两版**而不是改掉别人的。开 issue 请带上：你的环境（OS 版本、shell、工具版本）、你实测到的退出码/输出、你认为哪一节错了。维护者会按适用范围给两条都加注说明。

## 提交约定

- 一个 PR 只动一个主题，不顺手重排目录或改命名。
- **不改已推送的历史**（不 rebase 公共分支、不 `--amend` 别人能看到的提交）。
  - **一次性例外（落库 2026-09-19，裁定 2026-09-18）**：为清除公开历史里的**真实身份痕迹（即上面「四条红线」第 1 条所列那一类）**，`main` 被 `git filter-branch` 重写了一次。范围要精确：**19 处命中，分在 12 个载体对象**（11 个旧版本 blob + 1 条 commit message；按"词 × 载体对象"数是 14）。四类字面量换成占位符，其它内容一个字节都没动。
    本例外**只由"公开历史里出现红线第 1 条所列痕迹"触发**；"整理提交""合并主题""补历史戳"一律不算理由。越界判据写成硬标准：**改写前后 `main` 的 tip tree 必须逐字节相同**（本次两侧同为 `6e82677e…`）——凡是让内容发生变化的"改写历史"，都不在本例外范围内。
    手段上只用 `--index-filter` 与 `--msg-filter`，**不用 `--tree-filter`**：它要 checkout，会撞上上面那条 LF 规则而静默重排字节、改出另一套哈希。
    第 6 笔提交起 sha 全变（前 5 笔不含字面量），旧 tip 及其全部后代失效。协作者执行前先自证本地无未提交改动（`git status --porcelain` 必须为空），再 `git fetch && git reset --hard origin/main`；**不要 `git pull`**——pull 会把旧 DAG merge 回来，把刚清除的字面量重新带进历史。推送用 `--force-with-lease=main:<改写前 tip>`，把"远端仍是未改写状态"变成前提，期间有人抢先推就失败而不是静默覆盖；回滚路径是改写前那份 `git bundle create` 的完整备份（必要时 `git fetch <pre.bundle> main:refs/heads/main` 再按同样的 lease 推回）。
    这次买到的东西也要说准：**清除的是别人的真名与机器痕迹，不是"维护者是谁"**——仓库账号与用户 ID 仍在全部提交的 author/committer 里；且服务端在自身 GC 前仍可能按旧 sha 提供那些对象。这两项不在本例外能负责的范围内。
    - **验证于**：Windows 11 家庭中文版 10.0.26200 · Git Bash 5.2.37 · git 2.53.0.windows.2（本机无 `git filter-repo`）· Python 3.12.10 · 2026-09-19
- 文本一律 LF，交给 `.gitattributes` 管（Windows 上 Git 默认 `core.autocrlf=true`，不设这个文件会在 `git add` 时静默改字节）。
- 例外：含中文的 `.ps1` 示例需要 BOM，见 `windows-text-encoding §1`。
